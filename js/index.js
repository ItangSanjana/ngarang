(function ($) {
    /*
     the grids
     
    $(document).ready(function () {
        if ($("#gS").length === 0) {
            $("body").append($("<div id='gS'>" +
                "<div class='section clearfix'>" +
                "<div class='grid'>"));
            if ($("#gsToggle").length === 0) {
                $("#gS").after($("<a id='gsToggle' href='#'>x</a>"));
            }
        }
        var $grid = $("#gS").children("div.section").children();
        for (var i = 1, limit = 12; i < limit; i++) {
            $grid.clone().insertAfter($grid);
        }
        $("#gS").next("a").click(function () {
            $(this).prev().slideToggle("fast");
            return false;
        });
    });*/
})(jQuery);
